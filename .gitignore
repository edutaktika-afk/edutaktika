# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Build output (Netlify will build; uncomment to exclude deploy folder)
# deploy/

# Editor build output (now tracked for direct Netlify access)
# (If you want to stop tracking later, re-add: Editor/dist/ )

# Local environment / secrets
.env
.env.*
.env.local
.env.production
.env.development
# Never commit environment files with real credentials!

# OS / Editor junk
.DS_Store
Thumbs.db
.vscode/

# Archives / large binaries (comment out if you intend to serve them)
*.zip

# Temporary
tmp/
.temp/

# Exclude examples from repo if not needed in production (comment to include)
EXAMPLES/
